spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/energia
    username: root
    password: Un890341@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  flyway:
    enabled: false

application:
  security:
    jwt:
      secret-key: "c2VjcmV0LWtleS1mb3Itand0LXRlc3RpbmctZW52aXJvbm1lbnQtbXVzdC1iZS1sb25nLWVub3VnaA==" # Chave para testes
      expiration: 60000 # Tempo curto para testes (1 minuto)

server:
  port: 8081
