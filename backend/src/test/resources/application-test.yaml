spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/energia
    username: root
    password: Un890341@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  flyway:
    enabled: true
    clean-disabled: false
    baseline-on-migrate: true

server:
  port: 8081
